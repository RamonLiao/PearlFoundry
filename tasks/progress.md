# Progress — Structured Note Factory

## TODO（下一輪，建議新 chat）
- [x] **🟢 Mascot accents（kawaii 珍珠貝吉祥物前端點綴）✅ DONE + merge+push `origin/main`（2026-06-25，merge `9d51f80` --no-ff，feature branch `feat/mascot-accents` 已刪，push `d451ad3..5c198c8` ff 零 force 13 commits，local=remote 同步）**：把 `docs/logo-clear/` 三張 kawaii 珍珠貝 logo 變體加進前端。**核心張力＝細緻彩色卡通 raster vs refined Nacre Light 手繪 SVG 品牌 → 易 slop**。**定案＝hybrid treatment（飽和度=情緒，經 live 比較頁 `docs/logo-clear/treatment-compare.html` 驗證）**：空狀態 B nacre 雙調 always-on（沉穩 logo_3）、masthead A 灰階→點擊變彩 click-cycle [3,1,2]、mint 成功全彩+金光 `nl-clam-open`。**新檔**：`frontend/src/{Mascot.jsx,Mascot.css,mascot.js,mascot.test.js}`(pure helper `mascotSrc`/`treatmentClass` fail-loud + `MASCOT_VARIANT` 具名常數 SERENE/JOYFUL/SHOWY) + `frontend/public/logo_{1,2,3}-clear.png`。**改**：`{App.jsx,App.css,MyNotes.jsx,Leaderboard.jsx}`（masthead img→button 彩蛋、done-phase 慶祝 block、空狀態 `<p>`→`<figure>` duotone、consumer 樣式）。**共同處理**：殺 baked 卡通陰影（mask 淡 well 底）、well at-rest 安靜（molten 只留 mint）、reduced-motion gate、單一 keyframe `nl-clam-open`。**流程**：brainstorming(AskUserQuestion 鎖 4 插入點+hybrid treatment，live 比較頁讓使用者選 A/B)→2 路 plan-stage review(sui-frontend 抓 `<p>` 內塞 block 非法 HTML、`nl-reveal` keyframe 已雙重定義、done-phase 無 render site；frontend-design 抓 saturate(0.92)=no-op、殺 baked 雙陰影、logo_2 太炫不放空狀態、72px+真標題)→writing-plans 4-task→SDD（每 task haiku implementer + sonnet 前端 reviewer 雙驗，全 Spec✅ Quality Approved）→opus 全分支 READY TO MERGE 0 Crit/Imp→**dual-review codex --multi 5 passes**（correctness/security 全程 LGTM；修 R1 dead `reveal` abstraction+hoist、R2 拆 Mascot.css 自包含、**R3 真 correctness bug：masthead cycle off-by-one/dead element→統一 mascotSrc + parent --duotone modifier 取代 :has()**、R4 具名常數；triaged reject empty-state CSS 位置/改檔名=Rule 11 慣例；收斂判定 anti-infinite-retry）。frontend 40/40 + build green。**唯一 human-deferred＝瀏覽器真錢包 render**（吉祥物實際效果、慶祝動畫、masthead 輪播、空狀態；`cd frontend && npm run dev`）。spec `docs/superpowers/specs/2026-06-25-mascot-accents-design.md`、plan `…/plans/2026-06-25-mascot-accents.md`。比較頁 `docs/logo-clear/treatment-compare.html`（2MB base64，untracked 本地參考）。
- [x] **🟢 Sponsored-tx gas station（claim-only）✅ DONE + merge+push `origin/main`（2026-06-25，merge `029a535` --no-ff，feature branch `feat/sponsored-claim-gas-station` 已刪，push `9fc10df..029a535` ff 零 force）**：holder claim 免持 SUI——backend gas station sponsor-sign gas、holder 簽同一 bytes、前端 gRPC 雙簽提交，失敗 fallback 既有 self-pay。**機制 testnet probe 先驗**（兩 keypair 雙簽 + `grpc.core.executeTransaction` 真上鏈 digest `9ABVbVc6…`，Sender≠GasOwner）→ 才投實作。**新檔**：`scripts/integration/sponsor.js`（loadSponsor env fail-loud / pickGasCoins / signSponsored / `SPONSOR_GAS_CAP=20_000_000n`=0.02 SUI pinned）、`frontend/src/claimSponsored.js`（POST→signTransaction→**byte-equality assert**(C2 防 wallet 改 gasData)→gRPC execute，error 帶 `.phase` request/sign/verify/execute）+ 各 `.test.js`。**改**：`scripts/indexer/server.js`（`/sponsor-claim`+`/sponsor-status`+`assertClaimable`+sponsor 注入，guards 在 dry-run 前）、`frontend/src/{MyNotes.jsx,App.jsx,App.css}`（sponsored-first + phase fallback + onPhase 4-phase 按鈕 + gas-free chip + a11y）。**Red Team V1–V6**（owner guards before dry-run、CAP pinned client budget 忽略、byte-equality 防 bait-switch、replay 經 dry-run 擋、DoS 排序、coin race=liveness roadmap）。**流程**：brainstorming(AskUserQuestion×4 鎖 claim-only/自建 gas station/前端提交/sponsored+fallback)→2 路 plan-stage review(sui-architect 抓 **C1 JSON-RPC executeTransactionBlock 已被 P126 停用→改 gRPC**、C2 wallet 改 gasData 未知→byte-equality；frontend-design 補 gas-free chip/4-phase 按鈕/fallback 分支/a11y)→**testnet probe 證 C1+機制**→writing-plans 5-task→SDD（每 task fresh subagent 雙驗，Task4 抓 **Important: dead 'sponsoring' phase→加 onPhase callback** 修掉）→opus 全分支 **READY TO MERGE 0 Crit/Imp**（contract 一致、V1–V6 全防、無 dead branch、恰一 popup、self-pay 不變）→**dual-review codex --multi**（security LGTM；修 claimErrorCopy/inner-catch 非物件 deref；dismiss client/txdeps-503 false-positive=guard 在 server.js:153 diff 外）→re-review LGTM。backend 87/87 + integration 13/13 + pricing 21/21 + frontend 36/36 + build green。**human-deferred（非阻塞）＝瀏覽器真錢包**：C2 byte-equality probe（wallet 是否改 bytes，失敗則 forceSelfPay 兜底）+ sponsored round-trip（holder 0 SUI / sponsor gas-negative 帳本）。啟動需 `SPONSOR_KEY=suiprivkey... node scripts/indexer/server.js indexer.db 8787`（缺 key → /sponsor-status available:false → UI 靜默走 self-pay）。spec `docs/superpowers/specs/2026-06-24-sponsored-claim-gas-station-design.md`、plan `…/plans/2026-06-24-sponsored-claim-gas-station.md`。

- [x] **🟢 ③ `/quote`+`/claim-tx` 鏈上驗 `mgr→sender` ✅ DONE + LIVE 驗 + COMMIT/PUSH（2026-06-24；commit `3bcb046` + merge `9fc10df`，已 push `origin/main` tip `9fc10df`，clean+同步）**：write-path auth guard。**結論：on-chain 本就無 theft hole**（`predict::mint` + `predict_manager::withdraw` 都 owner-gated、note soulbound、wallet 必簽 sender）→ funds 不論 `mgr` 為何都安全。但 server 層原本「靠 dry-run mint 在 owner-gate abort」＝**incidental 保護**：(1) 非顯式、predict 改 gate 就靜默失效；(2) foreign/bogus `mgr` 要跑完整 `computeLadder`(多次 probe dry-run)才被拒＝**DoS/grief 面**；(3) `/claim-tx` 根本不 dry-run、任意 `mgr` 都回 tx；(4) 合法使用者 fat-finger 只拿到 opaque `QUOTE_DRYRUN_FAILED`。**修＝`assertManagerOwner(client,mgr,sender)`**：讀 `getObject(mgr)` → **exact type match** `config.PREDICT_MGR_TYPE`（= defining 原始 package addr `0xf5ea2b37…`，**跨 upgrade 不變**；suffix-only match 會被攻擊者自部署同名 `predict_manager::PredictManager` 繞過＝codex security 抓到的真 finding）+ `normAddr(owner)===normAddr(sender)`（雙邊 normalize 防 padded/unpadded 漂移）。掛在 `/quote`(computeLadder **之前**)+`/claim-tx` 頂端，403 `MGR_NOT_OWNED` / 400 `BAD_MGR` / 400 `BAD_PARAMS`(malformed sender) fail-fast。outer catch 加 `e.status` 透傳。**改檔**：`scripts/indexer/server.js`(+guard/normAddr/httpErr、2 route wire、catch)、`scripts/integration/config.js`(+`PREDICT_MGR_TYPE`)、`scripts/indexer/server.test.js`(+11 auth/monkey 測試、fixtures import config 常數)。**Red Team 5 向量**(foreign mgr/wrong-type/deleted/addr-form mismatch/malformed sender)全有測試。**流程**：read Move 合約+interface stub 確認 owner-gating→AskUserQuestion 敲範圍(Both)→實作→**dual-review codex --multi**：R1 抓 **suffix-match auth bypass(真)**+over-long comment+缺 monkey 測試（correctness 的「503 regression」=**false positive**，503 guard 在 route branch 之上、測試綠證偽）→修(exact type pin+trim+4 edge 測試)→R2 codex LGTM/LGTM+DRY(MGR_TYPE 改 import config)。**LIVE 驗(testnet 真 manager `0x29f2…fda4`)**：foreign sender→403 MGR_NOT_OWNED、CFG 物件當 mgr→400 BAD_MGR、**真 owner `0x1509…bc4c`→200 完整 quote(ladder+tx)**＝端到端通。indexer 80/80 + integration 6/6 + pricing 21/21 全綠。**無 human-deferred**（reject+admit 兩路徑都 live 驗）。**deferred ①②③ 全清空**。
- [x] **🔴 ② MyNotes/Leaderboard UX batch ✅ DONE + merge+push（2026-06-24，merge local `main` `727123a` --no-ff，feature branch `feat/ux-batch-mynotes-leaderboard` 已刪，已 push `origin/main` `7c865b2..727123a` ff 零 force）**：deferred ② 完成。**5 項純 presentation polish**：(1) `shortId(id,head=6,tail=4)` head…tail 截斷（破 leading-zero padded id `slice(0,12)` 顯示全 0；**新檔** `frontend/src/{format.js,format.test.js}`）套到 4 個 id 點（MyNotes note link、App pending/preview mgr、Leaderboard issuer `8,4`）；(2) Claim→`nl-btn--primary` + **table-scoped size override**（hero padding 會撐爆 52px row）；(3) expanded-row **pearl-chip close 鈕**（line-SVG X 非裸 ✕、focus-visible gold ring、stopPropagation 不回 toggle）；(4) **nacre-sweep load skeleton**（keyframe `nl-skel-sweep` 避撞既有 `nl-shimmer` chart、同表結構防 reflow、MyNotes 補 `loading` state + empty-state gate `!loading&&!msg` + sr-only role=status、3/4 列 per-col 變寬度）；(5) **rising-pearl mint loader**（棄旋轉環＝去 AI-slop、reuse `.nl-bubble` 漸層、molten 鈕用 `#3d1a28` 實色、min-width 防 layout shift）。全 reduced-motion **擴充既有 media block**（非新增）。**改檔**：`App.jsx/App.css/MyNotes.jsx/Leaderboard.jsx/Leaderboard.css`。spec `docs/superpowers/specs/2026-06-24-mynotes-leaderboard-ux-batch-design.md`、plan `…/plans/2026-06-24-mynotes-leaderboard-ux-batch.md`。**流程**：brainstorming(AskUserQuestion 敲 id 格式/skeleton 細緻度/範圍)→2 路 plan-stage design review(sui-frontend+frontend-design 抓 **C2 keyframe 命名碰撞=真 bug**、C1 旋轉環 AI-slop→pearl dots、skeleton 同表防 reflow、close glyph 線條化、Claim table-scoped、empty-state gate)→SDD 5-task(fresh subagent+spec/quality 雙驗，全 APPROVED；T4 2 Minor=Leaderboard skeleton 缺 !msg guard[unreachable]/separate-table[mutually-exclusive 無 reflow]→triage ACCEPTABLE)→opus 全分支 review(READY TO MERGE，0 Crit/Imp，token/keyframe/class 無碰撞、shortId 無漏點、state machine 三 branch 互斥無 stuck)→**dual-review codex R1 抓 shortId 短輸入閾值 `<=head+tail` vs spec `head+tail+1`（11-char id 假截斷）+ 測試假覆蓋邊界→修+boundary test→LGTM**。frontend 29/29（format +7）+ build green。**唯一 human-deferred＝瀏覽器實際 render（sandbox 無錢包）**：skeleton→loaded 過場、pearl-dot loader 在 molten 鈕、close-chip 位置。**剩 deferred ③（/quote 鏈上驗 mgr→sender）見下**。
- [x] **🔴 ① chart-as-centerpiece + de-AI 重設計 ✅ DONE（2026-06-24，merge local `main` `e049677` --no-ff，feature branch `feat/chart-centerpiece` 已刪，未 push）**：deferred ① 完成。**佈局 B+A 混合**＝masthead 下常駐 hero（idle 解說圖，**連錢包前就 render**＝demo 第一眼）→ 報價後原地變 2-col（hero chart `size="hero"` 640×360 + 6-欄 metric rail + Cancel/Confirm）。**動畫 A**＝階梯線 draw（1.1s）→ 填色+dots 從 0 淡入（無到有）→ 有限 shimmer（×3 settle，過 WCAG 2.2.2），全 reduced-motion gated。**去 AI**＝molten 實色主鈕（棄 pinkgold，#3d1a28 字 6.7:1）、glyph ✓/↗→inline SVG、容器 760→1040、metric rail＝分隔線清單非 6 卡、idle 概念＝inline definition rows。**新檔**：`frontend/src/{demoCurve.js,demoCurve.test.js,metricRail.js,metricRail.test.js,MetricRail.jsx}`；**改**：`PayoffChart.jsx`(hero/animated/illustrative props)、`App.jsx`(hero 重構+un-gate)、`App.css`、`mint.js`(notional 透傳)、`scripts/indexer/server.js`(+1 行 notional echo)+`.test.js`。spec `docs/superpowers/specs/2026-06-24-chart-centerpiece-redesign-design.md`、plan `…/plans/2026-06-24-chart-centerpiece-redesign.md`。**流程**：brainstorming(visual companion，layout/rail/anim 三輪互動)→2 路 plan-stage design review(sui-frontend+frontend-design，整合 hero size/不對稱 grid/divided-list rail/finite shimmer/un-gate/WCAG)→SDD 7-task(fresh subagent+spec/quality 雙驗，全 APPROVED)→opus 全分支 review(READY TO MERGE，零 Critical/Important，cross-cutting 接縫全綠無 dead-code)→dual-review(codex R1 抓 fmtExpiry RangeError→修+monkey test→LGTM)。frontend 22/22 + backend 69/69 + build green。**唯一 human-deferred＝瀏覽器實際 render（sandbox 無錢包）**，所有 data path headless 驗（notional/expiry 到 rail、單位正確）。**剩 deferred ②③ 見下**。

- [x] **🔴 payoff 經濟修正 A+B — A ✅、B-core ✅、B-leftover spike ✅ DONE + live 驗（2026-06-24，branch `fix/mint-claim-ui-tweaks` tip `b8008e0`）**：SDD 8-task，spec `docs/superpowers/specs/2026-06-24-payoff-leftover-spike-design.md`、plan `docs/superpowers/plans/2026-06-24-payoff-leftover.md`。**機制定案＝`leftover = net_principal − Σ PositionMinted.cost`**，全程 live 校準。**LIVE 雙重交叉驗證**（df-gone 重建路徑）：settled note 0xc03e leftover 5240530 = db payout 5240530（全 OTM）EXACT；0x9d56 leftover 7674254 + qty×1 ITM = 10997587 = db payout EXACT。final opus review NEEDS-FIXES（I1 settled row 不能展開 = Task4 重建路徑 prod 不可達）→ 修掉（`b8008e0`）+ M3 aria。backend 68/68、frontend build green。**唯一 human-deferred = 瀏覽器錢包 render（sandbox 無錢包），但所有 data path 已 live 驗**。下方原 spike 細節 ↓
  - **A ✅**＝refresh 孤兒 + quote 過期：`pendingMint.js`（per-address localStorage `{mgr,expiry,ts}`，try/catch safe）；`mint.js` 拆 `quoteMint()` + `prepareMint` 加 `onManager` hook（PTB1 落地即存 mgr，涵蓋 /quote 中途 refresh）；`App.jsx` resume banner（Resume＝重新 `/quote` 不重用舊 tx；Discard），pending 存在時禁用 Issue 鈕，resume 前 `isValidSuiObjectId` 守衛。dual-review pass、build green。
  - **B-core ✅**＝chart shift：`payoff.js` `computePayoffCurve` 收 optional `leftover`（default `0n`，抬 baseline/maxPayout，負值 fail-loud）；`PayoffChart.jsx` floor line/dots/settlement payoutAt/fill 全改用 `baseline+qtyPerLeg×k`。payoff 16/16（+5 leftover 測試）。**`leftover=0` ⇒ 畫面與現狀 pixel-identical，零退化**。
  - **B-leftover spike — live calibration ✅ DONE（2026-06-24），spec 寫好（commit `2500123`+chart-fix 更新）、進 writing-plans 中**：機制已 live 校準定案＝**`leftover = net_principal − Σ PositionMinted.cost`**（驗於 note `0x136990dd`/tx `pd7Mjqm…`：net 9.97M − Σcost 4.90M = **leftover 5.07 dUSDC**；cap=leftover+qty×legs=15.04，**= live `balance()` 實測值**，因該 note 全 ITM 結算）。**推翻 prior 假設**：`balanceChanges` 只回 −notional（sender-level，永不露 premium）✗；live `balance()` time-dependent（settled 後膨脹到 cap）✗。Source 統一＝mint tx events：`/quote` dry-run mint 加總 cost；`/note-params` 用 db `tx_digest` 抓 mint tx events（immutable、全生命週期通用，settled note 還能從 events 重建 staircase）。spec＝`docs/superpowers/specs/2026-06-24-payoff-leftover-spike-design.md`。
  - **chart 內部修正（折進本 spike）**：C1 baseline Y-tick（baseline>0 時 floor 無標 = 真 bug）、C2 forward marker rust 線/gold label 撞色統一、C3 settlement marker `--pearl` 浅底看不見→`--jade`、C4 `.nl-preview` overflow guard、C5 SVG max-width 480 vs CSS 420 撞值。
  - **（deferred 後續，獨立任務）**：① chart-as-centerpiece + de-AI 重設計（破 760px 單列、2-col chart+metric rail、nacre shimmer、staircase 入場、去 pink candy 按鈕、WCAG floor、glyph→SVG）→ 自己一輪 brainstorming；② MyNotes/Leaderboard UX batch（Claim→primary、expanded-row close、manager-id slice 全 0、loading skeleton、mint spinner）；③ `/quote` 鏈上驗 `mgr→sender`（codex security，write-path/auth，獨立）。兩路 review（sui-frontend + frontend-design/taste）findings 完整在本 session。
- [x] **🔴 mint `waitForTransaction` 硬化 ✅ live 驗過（2026-06-24）**：使用者真錢包 mint 成功、PTB2 不再 InsufficientGas。連同 payoff 圖修正一起 commit（見 Current Task）。
- [x] **🔴 payoff 圖 forward off-band + x 軸解析度 ✅ 修正（2026-06-24）— 見 Current Task**：forward 低於 band 時 clamp 進 plot 邊緣 + `<`/`>` 箭頭（原本畫到 plot 外壓住 y 軸 = 「圖亂」）；`fmtK` 改自適應小數位（窄 band 兩端不再都 `63k`，變 `62.63k`/`62.65k`）。build green、payoff 11/11。
- [x] **🔴 產品經濟學文件化 ✅（2026-06-24）**：`docs/product-economics.md`（入庫）+ memory `range-accrual-economics`。記錄：真實 payoff = `leftover + qty×ITM`（圖的 9.97 cap 是 bug，實證 settled note 5.24→11.0、可超 notional）、**誰付 payout = DeepBook Predict 對手方**（factory 非託管不補貼，FeeVault 無付款路徑）、對手方來源（Predict CLOB + SVI 定價）、產品特性（看漲方向、soulbound、self-settle、floor=leftover 非保本）、推薦對象/pitch。
- [x] **🔴 UI 全面重設計 ✅ 完成（2026-06-23）— 見 Current Task**：dark→light「Nacre Light」水底珍珠風，已 merge local `main`（merge `fcafb1f`，待 push）。
- [ ] **（前端互動候選）** sponsored-tx gas station / payoff diagram / 其他 strategy / 有梗的 empty+loading 狀態（final review N10 提的 faded clam + skeleton）。
- [x] **logo 最終定案 ✅（2026-06-23）**：使用者選 logo_3 去白底版。`frontend/public/logo-mark.png` ← logo_3-clear（透明底 342×341，corner flood-fill 去外圍白、中央圖案不動），`App.jsx:44` src → `/logo-mark.png`，build green。三張 clear 變體 + ondark 預覽整理在 `docs/logo-clear/`（含 README），**保留待之後加進前端讓畫面更活潑**（mint 動畫/空狀態插圖/loading 等）。舊 `logo-mark-tinted.png` 已無 caller，留作備份。
- [ ] **（前端活潑化，候選）** 把 `docs/logo-clear/` 三張 clear logo 變體加進前端互動點綴（見該資料夾 README 用途清單）。
- [x] **`MyNotes.claim()` stale re-query ✅ 修正（2026-06-23，commit `15baf13`，已 push）**：原 TODO 診斷為「缺 `waitForTransaction`」**有誤**——dapp-kit-react 2.x `signExec` await 已 resolve 在鏈上執行後，finality 不缺；真正 staleness 來源是**自架 off-chain indexer 的 polling lag**（`load()` 讀 `/notes`，`waitForTransaction` 只同步 fullnode 碰不到）。正解＝**optimistic 移除**剛 claim 成功的 note（`setNotes(prev=>prev.filter(...))`）取代 `await load()`，避免 indexer 還沒追上就把 claimed row 復活成 claimable；reconcile 交給 Refresh/useEffect。build green。
- [x] **payoff diagram ✅ 完成（2026-06-24）— 見 Current Task**：Range Accrual 收益階梯圖，mint 卡 preview + MyNotes 行內，merge local `main`（merge `706b76f` --no-ff，feature branch 已刪，**未 push**）。
- [ ] （候選）sponsored-tx gas station / 其他 strategy / empty+loading 狀態

## Blockers
- **無阻塞性 blocker**。deferred ② UX batch 完成、merge local `main` `727123a` --no-ff、**已 push `origin/main`**（tip `9b3e0d1`，含 ② code `7c865b2..727123a` + progress/lessons `727123a..9b3e0d1`，全 ff 零 force）。feature branch `feat/ux-batch-mynotes-leaderboard` 已刪。
- **唯一 human-deferred（非阻塞，② 與 ①）**：瀏覽器真錢包視覺 pass（sandbox 無錢包）——② 的 skeleton→loaded 過場 / pearl-dot loader 在 molten 鈕 / close-chip 位置；① 的 hero chart 入場動畫。`cd frontend && npm run dev` 可看。所有 data path 已 headless 驗。
- **（史）A+B**：merge `b2b2cd6` --no-ff、push `origin/main`（`2341f1e → b2b2cd6`，ff 零 force，20 commits）。
- **唯一 human-deferred（非阻塞）**：B-leftover 後的 **chart 在瀏覽器錢包實際 render**（sandbox 無錢包）。所有 data path 已 live 驗（含 settled note df-gone 重建路徑，數字對到鏈上 payout）。
- **後端 server 提醒**：`scripts/indexer/server.js indexer.db 8787` 只服務 HTTP **不跑 poller**，新 mint note 不自動 ingest，需另跑 `node scripts/indexer/ingest.js indexer.db`（或正式用 `keeper.js`）。
- **live-run（2026-06-24）抓到並修掉 2 個真 bug**：(1) **`/note-params` ParamsKey 編碼**：Move 空 struct 有 phantom `dummy_field: bool`，`value: {}` 被 RPC 拒(-32602)→ 改 `value: { dummy_field: false }`（commit `4150448`，live 確認 deleted note 現回乾淨 NO_PARAMS 404）。(2) **CORS**：server 從未送 `Access-Control-Allow-Origin`→ 瀏覽器擋掉所有 :8787 fetch（leaderboard/quote/note-params 全死）→ 加 CORS header + OPTIONS preflight（commit `dd9f719`，live 確認 leaderboard 載入）。
- **live mint round-trip ✅ 驗過（2026-06-24，使用者真錢包 `0xbdecf8a2…3ee01f`）**：mint 成功（tx `pd7Mjqm…`，note `0x136990dd…36d9`，16 legs alive）。**`/note-params` success 路徑 live 確認**：回真實 RangeParams（lower 62812e9/upper 62827e9/step 1e9/16 legs/qty 623125/forward 62825.3e9），證明 `rp=pf.value.fields ?? pf` nesting + dummy_field key 都對。**踩雷**：我啟的 `server.js` 只服務 HTTP **不跑 poller** → 新 note 不會自動進 db、MyNotes「No notes found」。需另跑 `node scripts/indexer/ingest.js indexer.db`（poller）才會 ingest；正式部署用 `keeper.js`（poller+watcher 合一）。
- **仍 deferred（次要）**：MyNotes 展開的 **compact 圖實際 render** 在使用者瀏覽器（我的 playwright 無錢包連不上 MyNotes）；claimable 結算落點需等該 note 到期結算後才看得到。資料路徑全綠。

## Recently Completed
- **2026-06-25 — 🟢 Mascot accents（kawaii 吉祥物前端點綴）✅ 完成 + merge local `main`（merge `9d51f80` --no-ff，feature branch 已刪，未 push）**：三張 kawaii 珍珠貝變體加進前端。hybrid treatment（飽和度=情緒）：空狀態 duotone always-on / masthead 灰階→點擊變彩 cycle / mint 全彩+金光。新檔 Mascot.{jsx,css}+mascot.{js,test.js}+3 PNG；改 App/MyNotes/Leaderboard。brainstorming(live 比較頁選 treatment)→2 路 plan-stage review(抓 `<p>` 非法巢狀、saturate(0.92)=no-op、殺 baked 雙陰影)→SDD 4-task(haiku+sonnet 雙驗)→opus READY TO MERGE→dual-review codex 5 passes(correctness/security 全 LGTM；R3 抓真 cycle off-by-one bug)。frontend 40/40+build green。human-deferred＝瀏覽器真錢包 render。詳見 TODO 頂。
- **2026-06-25 — 🟢 Sponsored-tx gas station（claim-only）✅ 完成 + merge+push `origin/main`（merge `029a535` --no-ff，tip `029a535`）**：holder claim 免持 SUI（backend sponsor gas + 雙簽 + gRPC execute，fallback self-pay）。**testnet probe 先證機制**（C1 gRPC dual-sig execute 真上鏈）才實作。SDD 5-task + onPhase fix + opus READY TO MERGE + dual-review LGTM。Red Team V1–V6 全防（owner guards before dry-run、CAP pinned、byte-equality C2）。87/87+13/13+21/21+36/36+build green。human-deferred＝瀏覽器真錢包 C2 probe + round-trip。詳見 TODO 頂。
- **2026-06-24 — 🟢 deferred ③ `/quote`+`/claim-tx` mgr→sender auth guard ✅ 完成 + LIVE 驗（未 commit）**：詳見 TODO ③。重點：on-chain 無 theft hole（owner-gated mint/withdraw + soulbound note），這層是 server 防禦深度 + DoS fail-fast + 清楚錯誤。`assertManagerOwner` exact-type-match `PREDICT_MGR_TYPE`(原始 defining addr 不漂移) + owner==sender(雙邊 normalize)。**codex --multi 抓到真 security finding：suffix-match 會被攻擊者同名 package 繞過→改 exact match**（503 regression finding 是 false positive）。LIVE：foreign→403、CFG→400、真 owner→200 完整 quote。107 tests 全綠。deferred ①②③ 清空。
- **2026-06-24 — 🟢 deferred ② MyNotes/Leaderboard UX batch ✅ 完成 + merge+push `origin/main`（merge `727123a` --no-ff，tip `9b3e0d1`）**：5 項純 presentation polish（shortId head…tail / Claim→primary table-scoped / pearl-chip close / nacre-sweep skeleton / rising-pearl mint loader）。新檔 `frontend/src/{format.js,format.test.js}`；改 `App.jsx/App.css/MyNotes.jsx/Leaderboard.jsx/Leaderboard.css`。**流程**：brainstorming(AskUserQuestion)→2 路 plan-stage design review(**抓 keyframe 命名碰撞 nl-shimmer 真 bug→nl-skel-sweep**、旋轉環 AI-slop→pearl dots)→SDD 5-task 雙驗全 APPROVED(T4 2 Minor triage ACCEPTABLE)→opus 全分支 READY TO MERGE(0 Crit/Imp)→**dual-review codex 抓 shortId 短輸入閾值偏離 spec(`<=head+tail` vs `head+tail+1`)+測試假覆蓋→修+boundary test→LGTM**。frontend 29/29 + build green、presentation-gate 空。spec/plan `docs/superpowers/{specs,plans}/2026-06-24-mynotes-leaderboard-ux-batch*`。詳見 TODO ②。
- **2026-06-24 — 🟢 B-leftover spike ✅ 完成 + live 交叉驗證、已 merge+push `origin/main`（merge `b2b2cd6` --no-ff，tip `b2b2cd6`）**：把**真 leftover** 餵進 payoff 圖。SDD 8-task（implementer+task review 每 task）+ opus 全分支 review。**機制 live 校準定案＝`leftover = net_principal − Σ PositionMinted.cost`**（mint tx events 是唯一 immutable 來源）。**推翻 2 個 prior 假設（皆 live 證偽）**：`balanceChanges` 只回 −notional（sender-level，永不露 premium）；live `predict_manager::balance()` time-dependent（settled 後膨脹到 cap）。`/quote` dry-run mint 加總 cost（兼 staleness guard，失敗回 502）；`/note-params` 用 db `tx_digest` 抓 mint tx events（全生命週期通用，claimed note 從 events 重建 staircase）。chart 修 C1 baseline floor tick（baseline>0 真 bug）+ C2/C3 marker 配色。final review 抓 I1（settled row 不能展開＝重建路徑 prod 不可達）→ 修（`b8008e0` 拿掉 3 個 `state!=='settled'` 守衛）。**LIVE 雙重交叉驗證**（df-gone 路徑）：0xc03e leftover 5240530 = db payout 5240530（全 OTM）EXACT；0x9d56 leftover 7674254 + qty×1 ITM = 10997587 = db payout EXACT。backend 68/68、frontend build green。spec `docs/superpowers/specs/2026-06-24-payoff-leftover-spike-design.md`、plan `…/plans/2026-06-24-payoff-leftover.md`。**兩路 design review（sui-frontend + frontend-design/taste）findings → deferred 3 批（見 TODO）**。
- **2026-06-24 — Payoff 經濟修正 A + B-core ✅（commit `f7f4938`，branch `fix/mint-claim-ui-tweaks`，未 push）**：**A**＝refresh 孤兒 + quote 過期 resume（`pendingMint.js` per-address localStorage；`mint.js` 拆 `quoteMint`+`onManager` hook；`App.jsx` resume banner + Issue-disable-while-pending + `isValidSuiObjectId` 守衛）。**B-core**＝`computePayoffCurve` 收 optional `leftover`（抬 baseline/maxPayout、負值 fail-loud、default 0n=零退化）+ `PayoffChart` floor/dots/settlement/fill 改用 `baseline+qtyPerLeg×k`。payoff 16/16、build green、dual-review pass。**B-leftover spike 仍待做**（需 live infra）。push 策略：A+B 全綠後一起 push。也順手 gitignore 了 `payoff-live*.png`。
- **2026-06-24 — UI 微調 + 2 live bug + mint gas 硬化 ✅ 完成、已 commit（branch `fix/mint-claim-ui-tweaks` `4a4ba95`，未 push）**：8 項（claim oracle asset bug、mint InsufficientGas 移除硬寫 budget + `waitForTransaction` 硬化、settled PnL、compact→full 圖、win-rate 置中、Issue 卡併排靠右、mint/claim/note hash 可點連結）。**claim live 驗過**（asset fix 後真錢包成功，−4.76 真虧損正確）；**mint waitForTransaction 硬化未 live 驗**。PTB1/PTB2 查證**不可合併**（predict 套件 create_manager 回 ID+內部 share、mint 吃 shared input）。詳見 Current Task + lessons。
- **2026-06-24 — Payoff diagram ✅ 完成 + live 全驗（merge local `main` `706b76f`，含 4 個 live-run fix，未 push）**：Range Accrual 收益階梯圖（mint 卡 preview + MyNotes 行內 compact）。SDD 6 task + opus 全分支 review（零 Critical/Important）+ 71/71 tests。**live-run 抓修 4 bug**：(1) `/note-params` ParamsKey 需 `value:{dummy_field:false}`（`4150448`）；(2) CORS header（`dd9f719`）；(3) ConnectButton 帳號下拉被卡片蓋（masthead `position:relative`+`z-index:20`，`b93c6a2`）；(4) [server.js 只服務 HTTP 不 ingest → 需另跑 `ingest.js`]。**使用者真錢包 live 驗過**：mint round-trip OK（note `0x136990dd…`）、MyNotes 16-leg compact 階梯圖 render 正確。commits 在 local `main` tip≈`b93c6a2`+progress，**全未 push**。
- **2026-06-23 — Nacre Light UI 重設計 ✅ 完成、merge local `main`（merge `fcafb1f` --no-ff，build green，feature branch 已刪，未 push）**：dark→light 水底珍珠風。
- **2026-06-23 — logo 定案 + Nacre 遷移 push ✅**：masthead logo = logo_3 去白底版（commit `a5b8d77`，build green）；Nacre Ledger 遷移已 push `origin/main`（`faef861`）。logo commit `a5b8d77` **待 push**（classifier 擋直推 default branch，使用者手動 push）。live masthead 截圖確認 logo 在暗背景乾淨融合，但使用者判定整體 UI「太醜」→ 下一輪重設計。
- **2026-06-23 — 整站 Nacre Ledger 遷移 ✅ 完成、已 merge 進 local `main`（merge `90c434a` --no-ff，build green）、已 push `faef861`**
- **2026-06-22 — demo deck（5-min 簡報）✅ 完成、已 push 到 `origin/main`（tip `76f142a`，ff 零 force）**
- **2026-06-22 — leaderboard 前端視圖（"Nacre Ledger"）✅ 完成、已 push 到 `origin/main`（tip `39c5612`，ff 零 force）**

## Current Task
**（無進行中任務）Mascot accents 完成、merge local `main` + push `origin/main`（merge `9d51f80` --no-ff，push `d451ad3..5c198c8` ff 零 force 13 commits，local=remote 同步，2026-06-25）**。唯一剩＝瀏覽器真錢包 render（吉祥物效果/慶祝動畫/masthead 輪播/空狀態；`cd frontend && npm run dev`，sandbox 無錢包）。下一輪候選：empty/loading 插圖更多點綴、其他 strategy（capped_upside / principal_protected）、把比較頁清掉或入庫。
- **（史）Sponsored-tx gas station 完成、merge+push `origin/main`（tip `029a535`，2026-06-25）**。C2 byte-equality probe + sponsored round-trip 為 human-deferred。
- **（史）deferred ①②③ 全部完成、已 commit + push `origin/main`（tip `9fc10df`，2026-06-24）**，working tree clean、local=remote 同步。indexer 80/80 + integration 6/6 + pricing 21/21（1 live skip）全綠。**下一輪候選**：sponsored-tx gas station（holder 簽 claim、項目方代付 gas）、`docs/logo-clear/` 三變體前端點綴、empty/loading 插圖、其他 strategy（capped_upside / principal_protected）。
- **本輪 ② 改動檔**：新增 `frontend/src/{format.js,format.test.js}`；改 `frontend/src/{App.jsx,App.css,MyNotes.jsx,Leaderboard.jsx,Leaderboard.css}`。
- **（史）B-leftover spike 改動檔**：新增 `scripts/indexer/{leftover.js,leftover.test.js}`；改 `scripts/indexer/{queries.js,server.js,server.test.js}`、`frontend/src/{mint.js,App.jsx,MyNotes.jsx,PayoffChart.jsx,theme.css,App.css,Leaderboard.css}`。
- **deferred 三批（獨立任務，下一輪挑）**：① **chart-as-centerpiece + de-AI 重設計**（破 760px 單列、2-col chart+metric rail、nacre shimmer、staircase 入場動畫、去 pink candy 主按鈕、WCAG floor `--chart-tick`/`--pearl-dim`/`--ink-faint`≥4.5:1、`✓`/`↗`→SVG）→ 自己一輪 brainstorming；② **MyNotes/Leaderboard UX batch**（Claim→primary、expanded-row close 鈕、manager-id `slice(0,12)` 在 padded id 全 0→改 last-8、loading skeleton、mint spinner）；③ **`/quote` 鏈上驗 `mgr→sender`**（codex security，write-path/auth）。
- **非阻塞 minor（SDD ledger）**：M1 `/quote` deriveLeftover throw→outer catch 500 非 502（仍 fail-loud）；M2 `dr.effects` 未 `?.` null-guard；M4 `/quote` 雙重 `tx.build`（perf trivial）。
- **驗證**：payoff 16/16（+5 leftover 測試）、`vite build` green、dual-review verdict = Ship as-is（codex 2 findings 修掉、1 intentional-deferred=leftover 未接 UI、1 backend-scope=`/quote` 應鏈上驗 mgr→sender 留 spike）。
- **關鍵決策**：`leftover` default `0n` ⇒ chart 與現狀 pixel-identical → B-core 可獨立 commit/不退化；leftover 真值來源（dryRun balanceChanges / BalanceManager bag）鏈上機制未證實，隔離成 spike 不擋已交付部分。
- **push 策略**：**先不 push**。下個 session 起 tx server + funded manager，一次 live 驗 gas硬化(舊債) + A resume + B 完整圖，A+B 全綠再一起 push。`origin/main` 仍停 `15baf13`。
- **未 live 驗**：A 的 refresh→resume→mint 真錢包流程；B-core 圖在 leftover>0 的實際 render。

---
**（史）UI 微調 + 2 live bug + mint gas 硬化 ✅ 完成、已 commit（2026-06-24，branch `fix/mint-claim-ui-tweaks` tip `4a4ba95`，未 push）**：在前一輪 6 項基礎上又加 2 項——(7) **claim 成功 URL 變可點連結**（前一輪只改了 mint URL，漏 MyNotes claim URL；`claimUrl` state + `nl-txlink`）；(8) **mint `InsufficientGas` 硬化**（live debug：查到使用者錢包 14.4 SUI/單一 coin，**非餘額問題**；`gasData.budget:null` 我的移除有效但仍 fail）→ 確診是「單一 SUI coin 連續兩簽」狀態競爭：PTB1 花掉唯一 coin 後，PTB2 gas 估算拿到舊版本 → 錢包誤報 InsufficientGas。修＝`prepareMint` 在 PTB1 成功後、`/quote` 前插 `client.waitForTransaction({digest, timeout:10_000})`（`App.jsx` 用 `useCurrentClient()` 取 dapp-kit gRPC client 傳入）。**best-effort 設計**（red-team 5 向量：無 digest→守衛跳過、hang→timeout、wait 拋錯但 PTB1 成功→吞掉不丟 manager 避免 orphan、錯網→用 wallet 同源 client、重複→idempotent）。**PTB1/PTB2 不可合併**（查證 Move：`predict::create_manager` 只回 `ID` 且內部 share，`mint_begin` 吃 `&mut PredictManager`=shared input；Sui 鐵則「同 PTB 現建 shared object 不能當同 PTB 的 shared input」+ probe 需先存在的 manager → 兩個獨立 blocker）。**11 檔 commit**（9 code + 2 tasks）。**⚠️ waitForTransaction 硬化未 live 驗**（playwright 連不上錢包，只 build green）。**claim live 驗過 ✅**：asset fix 後使用者真錢包 claim `0xc03e` 成功（payout 5.24−notional 10=**−4.76 真虧損**，range accrual 方向性產品結算落 band 中段、正確非 bug；perf_fee=0）。下方原 6 項 ↓

**UI 微調 + 2 live bug ✅（2026-06-24）**：使用者逐項截圖 feedback，6 項修正，純前端 + 1 行後端 query。**2 個真 bug（非 cosmetic）**：(1) **claim 失敗 `no oracle for asset=range_accrual`**——前端把產品類型 `n.strategy`(="range_accrual") 當 oracle 的 underlying asset 傳，但 `resolveOracle` 比對的是 `underlying_asset`(="BTC")。同一 bug 也讓 compact 圖 forward 線靜默讀失敗。修＝`MyNotes.jsx` 改傳 `UNDERLYING='BTC'` 常數（toggleExpand + claim 兩處）。(2) **mint 第二次簽署 `InsufficientGas`**——`txbuild.js buildMintTx` 寫死 `setGasBudget(2 SUI)` → dapp-kit **預留** 2 SUI gas coin，錢包 free SUI <2 就擋（實際 16-leg mint ~0.5 SUI）。修＝移除硬寫 budget，交給 dapp-kit dry-run 估算（CLI `mint.js:20` 自己另設 budget，不受影響；`txbuild.test.js` 的 `assert.ok(d.gasData.budget)` 反轉成 `assert.ok(!...)` 對齊新 intent + `buildCreateManagerTx` 既有慣例）。**4 個 cosmetic/info**：(3) 展開的 payoff 圖 `size="compact"`→`"full"`（detail row 滿版，給軸標/forward/settlement，跟 mint 預覽同款）；(4) **settled 行顯示已實現 PnL**——`queries.js listNotes` 多帶 `s.payout`/`s.perf_fee`（原本只取 settled 布林），前端 settled 行 Action 欄算 `payout−notional` 顯示綠/紅（DB 實證 `0x9d56` payout 10997587−notional 10000000=+1.00 dUSDC）；(5) win-rate 數字垂直置中——meter 改 `position:absolute` 浮數字下方（原 inline-flex column 坐 baseline 被頂高）+ `.nl-td vertical-align:middle`；(6) Issue 卡 address pill + Mint button 同行靠右（`.nl-issue-row justify-content:space-between`）+ mint 成功 URL/note hash 變可點連結（`EXPLORER_OBJ` 常數，object≠tx 路徑）。**test**：backend 83/83（1 live skip）+ frontend payoff 11/11 + `vite build` green。**後端已重啟**（`node scripts/indexer/server.js indexer.db 8787`，背景 task；schema 變更要重啟才生效——見 lessons）。**改動檔**：`frontend/src/{MyNotes.jsx,App.jsx,App.css,config.js,Leaderboard.css}`、`scripts/indexer/queries.js`、`scripts/integration/txbuild.js`+`.test.js`。**全未 commit**。

（前一任務）## Current Task
**payoff diagram ✅ 完成、merge local `main`（2026-06-24，merge `706b76f` --no-ff，feature branch `feat/payoff-diagram` 已刪，未 push）**：Range Accrual 收益階梯圖（payout vs settlement price，long up-ladder 單調上升階梯：結算價每越過一 strike payout+qty，<lower 全輸、≥upper 領滿）。**純 presentation-layer + read-only**（branch-wide `git diff` 證明 `move/`+indexer schema byte-unchanged；零合約/重部署/schema 改動）。**2 處接入**：(1) **mint 卡 preview** — 拆 `runMint`→`prepareMint`(PTB1+/quote)/`finalizeMint`(PTB2)，中間插 payoff 圖 + Confirm/Cancel gate（`mintPhase` state machine；Cancel 留可重用 manager）；(2) **MyNotes 行內** — 點列展開讀後端新增 `/note-params` route 畫 compact 圖（settled=已 claim=鏈上 note 已刪→不畫，只留 payout 數字）。**關鍵設計決策**：MyNotes 讀鏈走**後端 route**（符合既有 frontend→backend 慣例，瀏覽器從不直接讀鏈）→ 用 JSON-RPC `getDynamicFieldObject` 回 parsed fields，免瀏覽器 BCS（推翻 sui-frontend review 假設的 gRPC+browser-BCS）。`qtyPerLeg` 後端從權威 `fee_bps`(FactoryConfig) 算，client 不自算防漂移。**視覺 A×B hybrid**（nacre 4-stop 漸層填色 + molten 金階梯線 + strike 圓點 + rust forward 虛線 + 結算落點 ringed dot），WCAG tick/label、reduced-motion、`useId` gradient、legs>24 隱點、forward/settlement label 邊緣翻邊。**流程**：brainstorming(visual companion 鎖樣式)→writing-plans(6 task)→**2 路 spec design review（sui-frontend + frontend-design/taste）**抓 C1 raw-hex→token、C2 假 nacre 漸層(AI-purple)、C3 forward label 碰撞、WCAG、結構化結算標記 → 全 patch 進 spec。**SDD 6 task（fresh subagent + spec/quality 雙驗，3 個 fix wave）+ final whole-branch review（opus）= Ready-to-merge YES，零 Critical/Important**。`node --test` **71/71**（payoff 11 + qty 4 + indexer 56 含新 /note-params）、`vite build` green。**human-deferred（見 Blockers）**：live wallet round-trip + `/note-params` nesting 校準。spec `docs/superpowers/specs/2026-06-24-payoff-diagram-design.md`、plan `docs/superpowers/plans/2026-06-24-payoff-diagram.md`、SDD ledger `.superpowers/sdd/progress.md`。

（前一任務）## Current Task
**Nacre Light UI 重設計 ✅ 完成、merge local `main`（2026-06-23，merge `fcafb1f` --no-ff，feature branch `feat/nacre-light-redesign` 已刪，未 push）**：dark obsidian →light「Nacre Light」水底珍珠風（推翻上一輪暗色 fintech）。**純 presentation-layer，零 business-logic 改動**（branch-wide `git diff` 證明 api/mint/config/dapp-kit/main.js byte-unchanged；JSX 只動 markup/className/copy/SVG）。**brainstorming 用 visual companion 反覆對齊** → 使用者選「A 數據嚴謹+調色盤 × B 水底生命力」hybrid，視覺基準鎖 `docs/ui-redesign/v2d-hybrid.html`（v1→v2d 各版存檔）。**6 commits**：`3ad1154` theme.css token dark→light flip（keep 名翻值）+ index.html title/italic、`53bcf4d` `Sea.jsx`（caustic+10 rising bubbles 水底層）、`e8eb1ed` masthead（`PearlFoundry` wordmark em 實色 gold、bobbing logo、shell eyebrow SVG、mascot 行）+ mint 卡 pink-gold bouncy CTA、`b5a82ef` Leaderboard light retune（trophy SVG、rank-bar fix、保留全 6 欄 incl. Win Rate meter/Perf Fee）、`ee5639f` MyNotes clam SVG（Claim 維持 ghost）、`9ec9c12` reduced-motion fix。**6 個自繪 inline SVG ocean icon**（shell/wave/clam/trophy/bubbles/sparkle，`stroke=currentColor`、aria-hidden）取代 emoji。**流程**：brainstorming→writing-plans→**2 路 design review（sui-frontend + frontend-design/taste）**抓 WCAG CTA 對比 fail、dim text、gradient wordmark、nacre bar 搶戲、無 mobile、ConnectButton modal mismatch 等 → 全 patch 進 plan（標 `/* review */`）→ **SDD 5 task（fresh subagent + spec/quality 雙驗）**→ final whole-branch review（opus）= 1 Critical（logo bob 漏 reduced-motion）當場修。**live 視覺 gate PASS**（playwright dev server 截圖：亮珍珠底/bobbing logo/PearlFoundry 金字/Nacre Ledger trophy/氣泡/caustic；computed-style 確認 em 實色金無漸層）。`vite build` green。**已知 gap**（接受）：ConnectButton wallet modal 仍暗色（dapp-kit-react 2.x 無 theme prop，禁 selector-fish）。**deferred minor**：orphaned `--nacre-accent` token、duplicate `@keyframes nl-reveal`（無害）。spec `docs/superpowers/specs/2026-06-23-nacre-light-redesign-design.md`、plan `…/plans/2026-06-23-nacre-light-redesign.md`。

（前一任務）## Current Task
**整站 Nacre Ledger 遷移 ✅ 完成、已 merge local `main`（2026-06-23，merge `90c434a` --no-ff，feature branch 已刪，未 push）**：App.jsx / MyNotes.jsx / index.html / theme.css 全統一成 Leaderboard 的 obsidian+nacre+molten 風格。**純 presentation-layer**（sui-frontend skill 確認 SDK wiring 全對、零改動；business logic byte-identical）。**新增** `frontend/src/App.css`（masthead/card/btn--primary molten CTA/pill/typed status/status-pip/section reveal/reduced-motion）；**App.jsx** masthead（logo+eyebrow+h1+ConnectButton 包 `.nl-connect` 不碰內部）+ mint `.nl-card` + typed status；**MyNotes.jsx** 改 table（與 Leaderboard 同構，status pip settled/claimable/pending）。**3 cleanup**：`--brass`/`--nacre-accent` token + Leaderboard 去 hardcode hex、`<meta charset>` 置頂、**MyNotes `getNotes(normalizeSuiAddress(address))` address-normalize fix**（indexer 存 padded form、wallet 給 unpadded → 不 normalize 撈不到自己 notes）。**Logo**：logo_3 產 3 變體（去底/nacre-tint/手繪 SVG）+ 備份；default 用 tinted PNG（SVG 初版 render 成「眼睛」已重畫；ImageMagick 不會 render SVG gradient，瀏覽器正常）。**SDD 6 task（每 task fresh subagent + spec/quality 雙驗 review）+ final whole-branch review（opus）= Ready-to-merge YES 零 Critical/Important**。final review 4 Minor，當場修 2 個（MyNotes 補 `import './App.css'` 防 standalone 樣式破、masthead eyebrow 改 `Testnet · DeepBook Predict` 去 a11y 重複報讀）；剩 2 Minor（duplicate `@keyframes nl-reveal` body 相同無害、pnl===0 沿用舊行為）不處理。`vite build` green；computed-style 驗證（obsidian #0b0d12 + tinted logo loaded + Fraunces + nacre border + ConnectButton/leaderboard render）。**視覺限制**：playwright MCP 在隔離 sandbox，screenshot 取不回本機 → 改用 DOM/computed-style 探測當視覺 gate；live 外觀請 `cd frontend && npm run dev`。spec `docs/superpowers/specs/2026-06-23-nacre-ledger-sitewide-migration-design.md`、plan `docs/superpowers/plans/2026-06-23-nacre-ledger-sitewide-migration.md`。**未 merge/push，等使用者決定**（logo 定案 + merge 策略）。

（前一任務）## Current Task
**demo deck ✅ 完成、已 push 到 origin/main（2026-06-22，tip `76f142a`）**：hackathon 5-min 簡報素材。**2 檔 deliverable**：(1) `docs/demo-script.md` — 英式英文逐字稿，1min pitch / 3min live demo / 1min future vision；**誠實邊界**：demo 只演真正 live testnet 的閉環（Range Accrual mint→settle→claim + Nacre Ledger leaderboard），README 吹的 Walrus attestation / Capped-Upside / Principal-Protected / 白標 SDK **全歸 roadmap/vision 不 demo**（避免現場翻車）；含 fallback 錄影指引 + 可引用數字（16-leg mint≈0.505 SUI、claim gas-negative、fee vault 對帳到單位）。(2) `docs/slides.html` — 單檔自包含 10-slide deck，水平 nav（← →／左右點擊區／圓點跳頁／touch swipe／Home-End），過渡水平滑入 + prefers-reduced-motion；沿用 Nacre Ledger brand tokens（obsidian+nacre+molten、Fraunces+Martian Mono、fractalNoise 顆粒底）。10 張：Title→Problem→Solution→Demo intro→Mint→Claim→Ledger→Roadmap→Vision→Thanks。**委派 Gemini 失敗（daily quota 用盡）→ Claude 直接寫**。順手 gitignore `frontend/dist/`。logos `docs/logo_{1,2,3}.png` 一併入庫。commit `76f142a`，ff push（`39c5612..76f142a` 零 force/零分岔）。**未做（可選後續）**：slide 全螢幕鍵/自動計時/講者備忘稿模式。

（前一任務）## Current Task
**leaderboard 前端視圖（"Nacre Ledger"）✅ 完成、已 push 到 origin/main（2026-06-22）**：公開 issuer 排行榜（按 realized PnL），連錢包前可見、連上後 `normalizeSuiAddress` 兩邊比對高亮自己那行（`YOU` 標）。後端零改動（`/leaderboard` query 早已存在）。**3 檔新增/改**：`frontend/src/{Leaderboard.jsx,Leaderboard.css,theme.css}` + `api.js#getLeaderboard` + `index.html`(Fraunces+Martian Mono) + `App.jsx` mount（在 `{account &&}` guard 外）。**美學 = PearlFoundry brand 拆義素**：obsidian 黑曜底 + 珍珠母 nacre 虹彩 + 熔金 rank pip；Fraunces serif + Martian Mono tabular-nums；staggered reveal + prefers-reduced-motion；拒絕 AI-slop（無 Inter/紫漸層/generic card）。**範圍決策**：本輪共享 theme tokens 但只套 Leaderboard；**下一輪把整站（App/MyNotes/ConnectButton）遷移成 Nacre Ledger 統一風格（使用者明確要求）**。**SDD 2 task（fresh subagent + spec/quality 雙驗）+ final whole-branch review = opus Ready-to-merge YES + sui-frontend CONDITIONAL PASS**。Fix wave（commit 89a0991）：guard null issuer（normalize/slice crash→blank board）、`getLeaderboard` Array.isArray 防護、empty-state `&& !loading` 去閃爍。**Deferred 到下輪 migration**：`<meta charset>` 排序、2 個 hardcoded hex(#9c8045/#c9c1e8→token)、pnl===0 不顯示`+`(刻意)。spec `docs/superpowers/specs/2026-06-22-leaderboard-frontend-design.md`、plan `…/plans/2026-06-22-leaderboard-frontend.md`。**merge commit `da44fe6`（--no-ff），已 ff push 到 `origin/main`（tip `39c5612`，零 force/零分岔，local=remote 同步）**。`vite build` green。**下一輪候選：整站 Nacre Ledger 遷移 / sponsored-tx gas station / payoff diagram / 其他 strategy。**

（前一任務）## Current Task
**settlement watcher (notify-only daemon) ✅ 程式完成、未 push（2026-06-22）**：keeper 結算監看 + 通知 side-channel（A 自助 claim 路徑的主動 ping；keeper 結構性不能代簽 claim）。新增 `scripts/indexer/{notify,watcher,keeper}.js` + db `notified`+`meta` 表 + `pendingUnnotified` JOIN query（`pendingSettle`/`/pending-settle` 不動）。單進程雙 loop（runPoller 保鮮 db + runWatcher 通知）共用一個 better-sqlite3 handle + 一個 AbortController；`Promise.race` + `finally`-abort fail-loud 監督。**4 個 load-bearing 決策**：(1) 冷啟動用持久 `seed_cutoff_ts` 純 predicate 邊界（非 snapshot，免 poller race）；(2) log-before-mark（crash 不 silent-drop holder）；(3) webhook best-effort 3s local timeout、失敗吞掉不阻塞；(4) keeper 任一 loop 死就 abort 兩者+非零退出。**SDD 5 task（每 task fresh subagent + 雙驗）+ final whole-branch review（opus）= Ready-to-merge YES 零 Critical/Important**，55/55 全綠 + live CLI smoke（testnet ingest 5 events）。spec `docs/superpowers/specs/2026-06-22-settlement-watcher-design.md`、plan `docs/superpowers/plans/2026-06-22-settlement-watcher.md`。**Review 整合**：sui-indexer+sui-architect 審 spec 抓到冷啟動 snapshot-seed race（改 cutoff 邊界）+ mark/log 順序矛盾（改 log-before-mark）+ N+1→JOIN。**已整合進 remote**：fast-forward push 到 `origin/main`（github RamonLiao/PearlFoundry，tip `f57a352`，零 force/零分岔）；branch `feat/settlement-watcher` 早被 owner 改名成 `main`（含 PearlFoundry README b6694aa），stale `master`(03d0f21) 已刪。`indexer.db` 已加 gitignore。CLI：`WATCHER_WEBHOOK_URL=… node scripts/indexer/keeper.js indexer.db [--fire-backlog]`。**下一輪：sponsored-tx gas station（holder 簽 claim、項目方代付 gas）/ leaderboard 前端視圖 / payoff diagram / 其他 strategy。**

（前一任務）## Current Task
**frontend 兩顆按鈕（Mint+Claim）✅ 程式完成、已 merge master（2026-06-21）**：薄後端 + React dApp 閉環。後端擴充 `scripts/indexer/server.js` 3 條 POST 路由（`/create-manager-tx`、`/quote`、`/claim-tx`）回 **serialized 未-build PTB**，前端 `@mysten/dapp-kit-react` 2.x 錢包補 sender+gas 簽署（後端零 build、sender 永遠是參數）。PTB builder 抽成 `scripts/integration/txbuild.js`、`computeLadder` 加 sender；新增 `pickLiveExpiry`（自動選最近已定價未來 BTC expiry，免寫死）+ `coins.js`(dUSDC picker) + `/oracle` 路由。前端 `frontend/`：Mint 2-PTB（PTB1 create_manager→抽 MGR→/quote→PTB2，fail-loud）、My Notes 讀 `/notes`、每張到期 note 一顆 Claim。**SDD 8 task（每 task fresh subagent 實作+雙驗 review）+ final whole-branch review（opus）**，全綠：integration 6/6、indexer 37/37、pricing 16。9 commits + merge `fece704`。spec `docs/superpowers/specs/2026-06-21-frontend-two-buttons-design.md`、plan `.../plans/2026-06-21-frontend-two-buttons.md`。**關鍵 live 校準**：create_manager 確認只建 1 物件（Shared PredictManager）→ MGR positional 抽取無歧義；dapp-kit-react 2.x 簽署 API（`dAppKit.signAndExecuteTransaction`，結果 `{$kind,Transaction,FailedTransaction}` union，gRPC `effects.changedObjects` 非 JSON-RPC objectChanges）。**待辦（human）：瀏覽器錢包 live mint→claim round-trip + Monkey（後端 mint path 06-20 已 live 驗過 0.505 SUI）。下一步：settlement keeper / leaderboard 視圖(B) / payoff diagram(C) / 其他 strategy。**

（前一任務）**indexer 實作 ✅ 完成（2026-06-21）**：off-chain event-tailing indexer，merged to master（7 commits + merge）。3 層 `scripts/indexer/{events,db,queries,decode,ingest,server}.js`，**32 tests 全綠** + **live testnet 驗證**（5 events ingest，leaderboard PnL +997587、fees 對帳 132758 精確）。設計經 sui-architect + sui-indexer 兩輪 skill review、dual-review（codex 3 pass）整合。**關鍵 live 修正：filter 必須 `MoveEventModule` 非 `MoveModule`**（後者按 tx entry module=note_factory 過濾回 0；calibration gate 當場抓到）。ingestion 從 GraphQL 翻案成 JSON-RPC `suix_queryEvents`（GraphQL forward cursor 不可靠）；gRPC 列 roadmap。spec `docs/superpowers/specs/2026-06-21-indexer-design.md`、plan `.../plans/2026-06-21-indexer.md`。**下一步：選做 frontend 兩顆按鈕 / settlement keeper(接 /pending-settle) / 其他 strategy。**

（前一任務）**pricing-engine 實作 ✅ 完成（2026-06-20）**：Task 1–5 全綠，inline TDD。`scripts/pricing/{ladder,oracle,probe,price}.js` + 9 pure tests + 2 live probe tests 全過。端到端 `price.js`(16-leg)→`mint.js dryrun`=success，net gas **0.505 SUI**。4 個 plan 偏離全來自 live 實證（見 move-notes 2026-06-20 + lessons）：1-leg→2-leg 微 ladder、boundary maxLegs-capped、maxLegs 預設 16（gas 瓶頸非 band）、staleness guard 改 pre-submit dry-run。

（前一任務）**pricing-engine 設計+計畫 ✅ 完成（2026-06-20）**：spec `docs/superpowers/specs/2026-06-20-pricing-engine-design.md`、plan `docs/superpowers/plans/2026-06-20-pricing-engine.md`。

（前一任務）**Task 8 testnet 整合 ✅ 完成**：mint(2-PTB)→settle→claim happy-path 真實上鏈，5 Monkey abort 全過，FeeVault 對帳 132758。結算模式定案=兩者並存；MIN_NOTIONAL hackathon 1 / mainnet 5。

## TODO
- [x] **實作 plan Task 1–7**：events / fee_vault / leaderboard / note::soulbound_transfer / strategy_range_accrual / note_factory(mint+claim builder)。
- [x] **Move review chain**（move-code-quality → sui-security-guard → sui-red-team）整合 + 修正（見下方 Review Fixes）。
- [x] 查 testnet (BTC, 各 expiry) 的 `OracleSVI` object IDs → **解法=動態枚舉**（`suix_queryEvents` `registry::OracleCreated`）；ID ephemeral（15min 滾動）禁 hardcode。Task 8 blocker 解除。詳見 move-notes.md 2026-06-19。
- [x] **Task 8 整合測試 ✅ 完成**：
  - [x] 部署 package（digest AnPSTTKL…）；踩雷 `PublishUpgradeMissingDependency` → 補 DEEP+DeepBook linkage stub。
  - [x] PTB1 create_manager + mint 2-PTB **真實上鏈**（digest 13ikpdQK…，note 0x9d56…d929 soulbound）。
  - [x] 5 項 Monkey dry-run abort 全過（claim-before-expiry / dust / expiry-mismatch / >128 legs / 遠OTM）。
  - [x] claim happy-path **真實上鏈**（digest EveDmojuk…）：獲利結算 payout 10894829、perf fee 102758、note deleted、position 歸零。
  - [x] FeeVault 對帳 = 132758（30000 issuance + 102758 perf）精確。
  - 🔑 發現：`predict::mint` 有 ask-price band（strike 必須貼 forward，遠 OTM abort）。strategy 範圍須綁 forward 動態算（pricing-engine 職責）。
- [x] **結算模式決策 ✅ 定案（2026-06-19）**：**兩者並存**。實測 gas（mint +0.179 SUI / claim **−0.0072 SUI gas-negative 倒賺 rebate**）+ 修正框架：keeper「代執行 claim」**結構性不可能**（claim 吃 by-value owned soulbound note，Sui owned-object 只能 owner 簽 → 第三方碰不到）。合法兩路徑（holder 永遠 signer）：**A 自助 claim**（holder 付 gas）／**B sponsored tx**（holder 簽 + 項目方 gas station 代付 gas，免持 SUI）。合約零改動。mainnet 實踐 = indexer 監看結算 + gas station 服務 + 前端兩顆按鈕。詳見 move-notes.md 2026-06-19。
- [x] **MIN_NOTIONAL 決策**：與結算模式脫鉤，純擋 dust/spam。原 50–100 dUSDC 為臆測已推翻。**hackathon 維持 1 dUSDC**（現行 default，零治理動作）；**mainnet 前 `set_min_notional(5_000_000)` 拉 5 dUSDC**。
- [x] 設計決策：leg=N adjacent up-strike ladder、等權 sizing、claim 內 atomic settle+withdraw、最小 fee_vault+leaderboard、mint 2-PTB builder、roll/Defaulted 列 roadmap
- [x] sui-architect review（C1 soulbound_transfer / C2 public-fun-非-entry / A1 leaderboard event-only / A2 Display V2 roadmap）
- [ ] 後續：strategy_capped_upside、strategy_principal_protected、roll（shared-note 設計）、Defaulted 清算
- [x] **pricing-engine 實作 ✅（2026-06-20）**：Task 1–5 全綠，live 驗證 0.505 SUI mint。
- [~] off-chain 剩餘：Walrus 上傳、indexer、settlement-keeper、frontend 兩顆按鈕仍待設計

## Recently Completed (2026-06-19) — Task 8 testnet 整合
- **mint(2-PTB)→settle→claim 全 happy-path 真實上鏈**（mint `13ikpdQK…`、claim `EveDmojuk…`）。獲利結算 payout 10894829、perf fee 102758、note deleted、position 歸零。
- **FeeVault 對帳精確** = 132758（30000 issuance + 102758 perf）。
- **5 項 Monkey dry-run abort 全過**：claim-before-expiry / dust notional / expiry-count-mismatch / >128 legs / 遠 OTM strike。
- **部署** package `0xa699…b21a`（含 shared FactoryConfig/FeeVault、owned Admin caps + UpgradeCap）。commit `9fac24d`。
- **踩雷1 `PublishUpgradeMissingDependency`**：補 DEEP(`0x36dbef`)+DeepBook V3(`0x74cd56`) linkage-only stub（`move/deep_interface`、`move/deepbook_interface`），predict_interface 依賴之。
- **踩雷2 `predict::mint` ask-price band**：strike 必須貼 forward，遠 OTM abort（`assert_mintable_ask` code 7 / `pricing_config` code 1）。→ strategy 範圍須綁 live forward 動態算。
- **整合腳本** `scripts/integration/{config,mint,claim,poll_settle}.js`（@mysten/sui v1.36，oracle/mgr/note 走 env）。簽署用 keytool sign + execute-signed-tx（私鑰不離 keystore；export 被 classifier 擋＝正確）。

## Recently Completed (2026-06-20) — pricing-engine 實作 + dual-review
- **Task 1–5 全綠（inline TDD）**：`scripts/pricing/{ladder,oracle,probe,price}.js`。測試 **14 全綠**（10 pure: ladder+monkey；4 live testnet: oracle+probe）。
- **端到端 live**：`price.js`(16-leg)→`mint.js dryrun`=**success，net gas 0.505 SUI**；`bytes`+`GUARD=1` pre-submit dry-run 通過。
- **4 個 plan 偏離（全 dry-run 實證，詳見 move-notes 2026-06-20 + lessons）**：
  1. 1-leg 探測非法（`legs_per_expiry` assert `lower<upper`）→ 改 **2-leg 微 ladder**（partner 往 forward 靠，單調性隔離被測 strike）。
  2. band 寬 ~6000-8000 ticks ≫ MAX_LEGS → boundary **maxLegs-capped**（`findBoundary` 回 `{strike,capped}`）。
  3. gas 隨 leg 陡升（16=0.5 / 32=1.6 / 128>10 SUI 超錢包）→ off-chain **maxLegs 預設 16**（你拍板）；單 PTB 瓶頸是 gas 非 band；mint.js gasBudget→2 SUI。
  4. oracle ts 連續更新、forward probe 期間抖 ~20 ticks 但永不掉出寬 band → staleness guard 改 **pre-submit dry-run**（`GUARD=1`，無 magic 閾值）。
- **fetchOracle 加 forward==0 fail-loud**（剛建立未定價 oracle）。
- **dual-review 修 3 真 bug**：(F1) shrink clamp 進 probed band（不對稱 band 防 out-of-band leg）+回歸測試；(F2) 非 2 次方 kmax 精確 probe cap；(F3) oracle.test 篩 BTC event 去 flaky。
- **commits**：cab25e2 / 3c8c50b / cf84c92 / fc094f7 / 689e084 / c67b111 + review-fix。

## Recently Completed (2026-06-20) — pricing-engine 設計+計畫
- **brainstorming 4 決策**：(1) band 判定走 **dry-run 探測**（非 SVI 複刻，符合 lessons「runtime gate 要實證」）；(2) 交付 = **CLI 函式模組** `scripts/pricing/`，mint.js import；(3) forward 來源 = **oracle 為主**（讀 `prices.forward`）+ PositionMinted event 為 sanity；(4) 目標 = **最大化合法寬度**。
- **驗證 OracleSVI schema（live testnet）**：`prices.{forward,spot}`、`svi.{a,b,m…}`、`settlement_price`(null=未結算)、`expiry`、`timestamp`；`tick_size`/`min_strike` 在 `registry::OracleCreated` event 不在 object content。
- **sui-architect review 整合 A1–A4 進 spec**：A1 probe **非無狀態**（mint_begin 先扣 notional/splitCoins 早於 band check → probe 需 funded coin+MGR+固定 notional）；A2 **ladder staleness TTL**（forward 15min 滾動，compute-then-mint-immediately，mint.js re-fetch guard）；A3 JSON-RPC deprecated 技術債註記（dryRun ≠ Quorum Driver，hackathon OK）；A4 event sanity 僅數量級、權威邊界以 dry-run 為準。
- **產出**：spec `docs/superpowers/specs/2026-06-20-pricing-engine-design.md`、plan `docs/superpowers/plans/2026-06-20-pricing-engine.md`（5 tasks：ladder 純函式→oracle→probe→price CLI+mint guard→monkey）。
- **校準風險（plan 已標）**：`parseAbortCode` regex 要對真實 SDK error string 驗一次再依賴（Task 3 Step 4）。

## Recently Completed (2026-06-19) — Task 1–7
- **實作 Task 1–7**：6 個 module 全部從 scaffold 寫到完整。純函式 TDD（fee_vault / strategy sizing / factory fee）+ Predict-touching 路徑 build-check only。
- **三輪平行 review 整合修正**（red-team 另留 `tests/red-team/red_team_money_flow.move` 9 攻擊測試）：
  - **F1** `mint_begin` 加 `assert expiry_total > 0`（擋不受控 div-by-zero）。
  - **F2** `legs_per_expiry` 加 `MAX_LEGS=128` 上限 + `ETooManyLegs` domain error（擋 DoS/PTB gas 爆 + 不受控 cast abort）。red-team round_8/8d 翻成預期 abort；補邊界測試 128 接受/129 拒絕。
  - **F3** `FactoryConfig.min_notional`（可調欄位，預設 1_000_000=1 dUSDC）+ `set_min_notional` cap-gated setter；`mint_begin` assert `notional >= min_notional`（擋 0-fee dust 鑄造 + leaderboard spam）。
  - **F4** `mint_begin` assert `hurdle_bps >= 10000`（鎖死「績效費只收回本以上獲利」）。
  - **F5** vector free-fn → Move 2024 method syntax（~8 處）。
  - 補 `set_paused` setter（原 `paused` 欄位被檢查卻無處可設＝dead switch）。
  - 順手清掉 fee_vault deprecation warning（`with_defining_ids` / `exists_with_type`）、drop `mint_finalize` 未使用 `<T>`。
- **與計畫偏離**：`mint_finalize` 移除 `<T>`（整合測試 PTB 呼叫不帶 typeArg）。
- **未改（review 確認為設計/可接受）**：access-control（soulbound by-value）、manager/oracle/note binding、partial-settle 原子性、perf-fee ≤9 unit 進位 dust、claim 提走整個 manager balance（依「一 note 一 manager」不變式，無法鏈上 assert，列已知前提）。

## Recently Completed (2026-06-18)
- **驗證 single-PTB mint 結構性不可能**（撈 testnet ABI）：`predict::create_manager` 與 `predict_manager::new` 都只回 `ID`、內部 share manager；PTB 無「ID→&mut shared」command，shared input 須簽章時指定 → **解掉舊 TODO「同-PTB shared manager」，答案=不行，mint 拆 2-PTB**。0 gas。
- **驗證 keeper auto-roll 不可能**：mint owner-gated + note soulbound owned → keeper 兩者都碰不到。roll 改列 roadmap。
- 確認 `predict::mint`/`redeem_permissionless` 每次吃單一 `&OracleSVI` → 多 expiry 必須 hot-potato builder（Move entry 不能吃 `vector<&ref>`）。
- 產出 design：`docs/specs/2026-06-18-range-accrual-issue-settle-design.md`（修正舊 spec §5.1 single-PTB、§3 claim 歸屬）
- 產出 plan：`docs/superpowers/plans/2026-06-18-range-accrual-issue-settle.md`（8 tasks，純函式 TDD + testnet 整合）
- `git init` + `.gitignore`（排除 .claude/、CLAUDE.md、memory/、move/build/），初始 commit `a9c07e4`

## Blockers
- **無 blocker**。Task 1–8 全完成；OracleSVI 動態枚舉已落地、round-trip 全驗證。
- 待決策（非 blocker）：結算模式（自助 claim vs keeper 代付）。

## Notes
- **架構定案**：mint = 2 PTB。PTB1 raw `predict::create_manager`（owner=holder）；PTB2 = `mint_begin → mint_add_expiry×N → mint_finalize`（no-ability hot-potato `MintTicket`）。claim = `claim_begin → claim_settle_expiry×N → claim_finalize`（`ClaimTicket`，atomic settle+withdraw，post-check position==0）。全為 `public fun` 非 `entry`（C2）。
- **C1**：`NoteBase` key-only，跨 module 轉移只能經新增的 `note::soulbound_transfer`（factory 不能直接 `transfer::transfer`/`public_transfer`）。
- **A1**：leaderboard 無 on-chain vector/shared 物件，`register` 純 emit event；ranking 走 off-chain indexer。
- **payoff/sizing**：等權 ladder，`legs_per_expiry=(upper-lower)/step+1`，`qty_per_leg=net/(lpe·expiry_count)`。perf fee=10%，hurdle 預設 10000bps（net principal）。fee kind：0 issuance / 1 perf。
- **關鍵鏈上事實**：Predict 無 Position 物件；部位記在 shared `PredictManager`（key-only 無 store），key=MarketKey/RangeKey。
- **Addresses (testnet)**：Predict pkg `0xf5ea…5138`；shared obj `0xc8736…028a`；dUSDC `0xe95040…3e1a::dusdc::DUSDC`。
- 細節全在 design doc + plan + `move-notes.md`。下一個 Move 任務（實作）開新 chat（conventions：一 chat 一任務）。
