/// Capped Upside template: leg composition + payoff/settlement math.
/// Provides one-time witness `CappedUpside` and versioned params.
/// See spec §3 (strategy_* modules) and §4.
module structured_note_factory::strategy_capped_upside;
