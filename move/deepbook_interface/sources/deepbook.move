// Linkage-only placeholder. NEVER deployed (published-at points at the live
// on-chain DeepBook V3 package). Body intentionally empty — only the package's
// published-at / address mapping is needed for the downstream linkage closure.
module deepbook::deepbook_stub;
